# CLI-Projects Command Flows

## Overview
This document outlines the user flows for each command in the CLI-Projects tool.

---

## 1. `projects init`

**Purpose**: Initialize configuration and set up the tool for first use

**Flow**:
```
┌─────────────────────────────────┐
│ User runs: projects init        │
└────────────┬────────────────────┘
             │
             ▼
┌─────────────────────────────────┐
│ Create config file at           │
│ ~/.config/cli-projects/config.yml│
└────────────┬────────────────────┘
             │
             ▼
┌─────────────────────────────────┐
│ Prompt: Install shell           │
│ completions? (Y/n)              │
└────────────┬────────────────────┘
             │
      ┌──────┴───────┐
      │              │
     Yes            No
      │              │
      ▼              ▼
  Install       Skip setup
  tabtab           │
  completions      │
      │            │
      └──────┬─────┘
             │
             ▼
┌─────────────────────────────────┐
│ Show next steps:                │
│ 1. Add a group: projects add    │
│ 2. List groups: projects list   │
└─────────────────────────────────┘
```

**Output**: Config file created, optional completions installed

---

## 2. `projects add [group-name] [base-path]`

**Purpose**: Add a new project group

**Flow A: Interactive Mode** (no arguments):
```
┌─────────────────────────────────┐
│ User runs: projects add         │
│ (in a directory)                │
└────────────┬────────────────────┘
             │
             ▼
┌─────────────────────────────────┐
│ Use current directory as base   │
│ Suggest name from dir name      │
└────────────┬────────────────────┘
             │
             ▼
┌─────────────────────────────────┐
│ Prompt: Group name?             │
│ Default: [DIRECTORY_NAME]       │
└────────────┬────────────────────┘
             │
             ▼
┌─────────────────────────────────┐
│ Scan current directory for      │
│ subdirectories (skip hidden)    │
└────────────┬────────────────────┘
             │
             ▼
┌─────────────────────────────────┐
│ Show checkbox list:             │
│ Select projects to include:     │
│ ☑ project-1                     │
│ ☑ project-2                     │
│ ☑ project-3                     │
└────────────┬────────────────────┘
             │
             ▼
┌─────────────────────────────────┐
│ Save group to config            │
│ Show: ✓ Added group: NAME       │
│       Base path: /path          │
│       Projects: 3               │
└─────────────────────────────────┘
```

**Flow B: With Arguments**:
```
┌─────────────────────────────────┐
│ User runs: projects add         │
│ FRONTEND ~/Dev/vue              │
└────────────┬────────────────────┘
             │
             ▼
┌─────────────────────────────────┐
│ Use provided group name & path  │
│ No automatic project scanning   │
└────────────┬────────────────────┘
             │
             ▼
┌─────────────────────────────────┐
│ Save group to config            │
│ Show: ✓ Added group: FRONTEND   │
│       Base path: ~/Dev/vue      │
│       No projects added         │
└─────────────────────────────────┘
```

**Flow C: With --scan flag**:
```
┌─────────────────────────────────┐
│ User runs: projects add         │
│ FRONTEND ~/Dev/vue --scan       │
└────────────┬────────────────────┘
             │
             ▼
┌─────────────────────────────────┐
│ Scan directory automatically    │
│ Add ALL subdirectories          │
└────────────┬────────────────────┘
             │
             ▼
┌─────────────────────────────────┐
│ Save group with all projects    │
│ Show: Found 17 projects         │
│       ✓ Added group: FRONTEND   │
└─────────────────────────────────┘
```

---

## 3. `projects add-project <group> [project]`

**Purpose**: Add a single project to an existing group

**Flow**:
```
┌─────────────────────────────────┐
│ User runs: projects add-project │
│ FRONTEND [project-name]         │
└────────────┬────────────────────┘
             │
      ┌──────┴───────┐
      │              │
  No project     Project name
   provided       provided
      │              │
      ▼              │
┌───────────────┐    │
│ Use current   │    │
│ directory     │    │
│ name as       │    │
│ project name  │    │
└──────┬────────┘    │
       └──────┬──────┘
              │
              ▼
┌─────────────────────────────────┐
│ Add project to group config     │
│ Show: ✓ Added project: name     │
│       Group: FRONTEND           │
└─────────────────────────────────┘
```

**Example**:
```bash
cd ~/Dev/vue/my-app
projects add-project FRONTEND
# Adds "my-app" to FRONTEND group

projects add-project FRONTEND other-app
# Adds "other-app" to FRONTEND group
```

---

## 4. `projects list [group]`

**Purpose**: List all groups or show details of one group

**Flow A: List All**:
```
┌─────────────────────────────────┐
│ User runs: projects list        │
└────────────┬────────────────────┘
             │
             ▼
┌─────────────────────────────────┐
│ Load config, get all groups     │
└────────────┬────────────────────┘
             │
             ▼
┌─────────────────────────────────┐
│ Display:                        │
│ 📁 Project Groups:              │
│                                 │
│ FRONTEND                        │
│   Base: ~/Dev/vue               │
│   Projects: 17                  │
│                                 │
│ BACKEND                         │
│   Base: ~/Dev/api               │
│   Projects: 7                   │
└─────────────────────────────────┘
```

**Flow B: Show One Group**:
```
┌─────────────────────────────────┐
│ User runs: projects list        │
│ FRONTEND                        │
└────────────┬────────────────────┘
             │
             ▼
┌─────────────────────────────────┐
│ Load group details              │
└────────────┬────────────────────┘
             │
             ▼
┌─────────────────────────────────┐
│ Display:                        │
│ 📁 Group: FRONTEND              │
│ Base: ~/Dev/vue                 │
│ Projects: 17                    │
│                                 │
│   ✓ accounts-app                │
│   ✓ dashboard-app               │
│   ✗ old-app (missing)           │
│   ...                           │
└─────────────────────────────────┘
```

---

## 5. `projects manage [group] [project]`

**Purpose**: Interactively manage groups and projects

**Flow A: Manage Group** (no project specified):
```
┌─────────────────────────────────┐
│ User runs: projects manage      │
│ [group]                         │
└────────────┬────────────────────┘
             │
      ┌──────┴───────┐
      │              │
  No group      Group name
   provided      provided
      │              │
      ▼              │
┌───────────────┐    │
│ Show list:    │    │
│ Which group?  │    │
│ > FRONTEND    │    │
│   BACKEND     │    │
└──────┬────────┘    │
       └──────┬──────┘
              │
              ▼
┌─────────────────────────────────┐
│ Show group actions menu:        │
│ 📁 Managing Group: FRONTEND     │
│ Base: ~/Dev/vue                 │
│ Projects: 17                    │
│                                 │
│ What would you like to do?      │
│ > 📋 Manage projects            │
│   ✏️  Rename group              │
│   �️  Delete group              │
│   ✓ Done                        │
└────────────┬────────────────────┘
             │
     ┌───────┼───────┬──────┬──────┐
     │       │       │      │      
  Manage  Rename  Delete  Done
  Projects Group   Group     │
     │       │       │       │
     ▼       ▼       ▼       ▼
  [See 5a] [See 5b] [See 5c] Exit
```

**Flow B: Manage Project** (project specified):
```
┌─────────────────────────────────┐
│ User runs: projects manage      │
│ FRONTEND my-app                 │
└────────────┬────────────────────┘
             │
             ▼
┌─────────────────────────────────┐
│ Go directly to project actions  │
│ (Skip group menu)               │
└────────────┬────────────────────┘
             │
             ▼
     [See 5d - Project Actions]
```

### 5a. Manage Projects Sub-flow:
```
┌─────────────────────────────────┐
│ Show list of all projects:      │
│ Select a project to manage:     │
│ > ✓ accounts-app                │
│   ✓ dashboard-app               │
│   ✗ old-app (missing)           │
│   ─────────────                 │
│   ← Back                        │
└────────────┬────────────────────┘
             │
      ┌──────┴───────┐
      │              │
   Select        Back to
   project       group menu
      │              │
      ▼              │
┌─────────────┐      │
│ Go to       │      │
│ project     │      │
│ actions     │      │
│ [See 5d]    │      │
└──────┬──────┘      │
       └──────┬──────┘
              │
              ▼
     [Back to group menu]
```

### 5b. Rename Group Sub-flow:
```
┌─────────────────────────────────┐
│ Prompt: Enter new group name:   │
│ Default: [current name]         │
└────────────┬────────────────────┘
             │
             ▼
┌─────────────────────────────────┐
│ Validate:                       │
│ - Not empty                     │
│ - Different from current        │
│ - Not already exists            │
└────────────┬────────────────────┘
             │
      ┌──────┴───────┐
      │              │
    Valid       Invalid
      │              │
      ▼              ▼
  Rename group   Show error
  Update name    Try again
      │              │
      ▼              │
┌─────────────┐      │
│ Show: ✓     │      │
│ Renamed:    │      │
│ OLD → NEW   │      │
└──────┬──────┘      │
       └──────┬──────┘
              │
              ▼
     [Back to group menu]
```

### 5c. Delete Group Sub-flow:
```
┌─────────────────────────────────┐
│ Prompt: Delete group "NAME"?    │
│ This will not delete actual     │
│ project directories. (y/N)      │
└────────────┬────────────────────┘
             │
      ┌──────┴───────┐
      │              │
     Yes            No
      │              │
      ▼              ▼
  Delete group   Cancel
  from config      │
      │            │
      ▼            │
┌─────────────┐    │
│ Show: ✓     │    │
│ Deleted     │    │
│ group: NAME │    │
└──────┬──────┘    │
       │           │
       └─────┬─────┘
             │
             ▼
           Exit
     (group is gone)
```

### 5d. Project Actions Sub-flow:
```
┌─────────────────────────────────┐
│ Show project menu:              │
│ � Managing Project: my-app     │
│ Group: FRONTEND                 │
│ Path: ~/Dev/vue/my-app          │
│ Status: ✓ Exists                │
│                                 │
│ What would you like to do?      │
│ > ✏️  Rename project            │
│   🗑️  Remove from group         │
│   ✓ Back                        │
└────────────┬────────────────────┘
             │
      ┌──────┼───────┬──────┐
      │      │       │      │
   Rename  Remove  Back   
   project project   │
      │      │       │
      ▼      ▼       ▼
  [See 5e] [See 5f] Exit
```

### 5e. Rename Project Sub-flow:
```
┌─────────────────────────────────┐
│ Prompt: Enter new project name: │
│ Default: [current name]         │
└────────────┬────────────────────┘
             │
             ▼
┌─────────────────────────────────┐
│ Validate:                       │
│ - Not empty                     │
│ - Different from current        │
│ - Not already in group          │
└────────────┬────────────────────┘
             │
      ┌──────┴───────┐
      │              │
    Valid       Invalid
      │              │
      ▼              ▼
  Rename in     Show error
  config        Try again
      │              │
      ▼              │
┌─────────────┐      │
│ Show: ✓     │      │
│ Renamed:    │      │
│ OLD → NEW   │      │
└──────┬──────┘      │
       └──────┬──────┘
              │
              ▼
     [Back to project menu]
```

### 5f. Remove Project Sub-flow:
```
┌─────────────────────────────────┐
│ Prompt: Remove "project" from   │
│ group "GROUP"? This will not    │
│ delete the actual directory.    │
│ (y/N)                           │
└────────────┬────────────────────┘
             │
      ┌──────┴───────┐
      │              │
     Yes            No
      │              │
      ▼              ▼
  Remove from    Cancel
  group config     │
      │            │
      ▼            │
┌─────────────┐    │
│ Show: ✓     │    │
│ Removed     │    │
│ project     │    │
└──────┬──────┘    │
       │           │
       └─────┬─────┘
             │
             ▼
           Exit
   (back to group menu)
```

**Usage Examples**:
```bash
# Interactive group selection
projects manage

# Manage specific group
projects manage FRONTEND

# Manage specific project directly
projects manage FRONTEND my-app
```

---

## 6. `projects run <group> [command...]`

**Purpose**: Execute a command across all projects in a group

**Flow**:
```
┌─────────────────────────────────┐
│ User runs: projects run         │
│ FRONTEND git status             │
└────────────┬────────────────────┘
             │
             ▼
┌─────────────────────────────────┐
│ Load group config               │
│ Get list of projects            │
└────────────┬────────────────────┘
             │
             ▼
┌─────────────────────────────────┐
│ For each project in group:      │
│                                 │
│ 1. Show: Running in [project]   │
│ 2. cd to project directory      │
│ 3. Execute command              │
│ 4. Show output                  │
│ 5. Show success/error           │
└────────────┬────────────────────┘
             │
             ▼
┌─────────────────────────────────┐
│ Show summary:                   │
│ ✓ Completed: 15/17 projects     │
│ ✗ Failed: 2 projects            │
└─────────────────────────────────┘
```

**With --dry-run flag**:
```
┌─────────────────────────────────┐
│ User runs: projects run         │
│ FRONTEND git pull --dry-run     │
└────────────┬────────────────────┘
             │
             ▼
┌─────────────────────────────────┐
│ Show what WOULD be executed:    │
│                                 │
│ [DRY RUN] accounts-app          │
│ $ git pull                      │
│                                 │
│ [DRY RUN] dashboard-app         │
│ $ git pull                      │
│ ...                             │
└─────────────────────────────────┘
```

**With --no-cd flag**:
```
┌─────────────────────────────────┐
│ User runs: projects run         │
│ FRONTEND --no-cd ls             │
└────────────┬────────────────────┘
             │
             ▼
┌─────────────────────────────────┐
│ Execute command from base dir   │
│ (not in each project)           │
│                                 │
│ Running from: ~/Dev/vue         │
│ $ ls                            │
│ accounts-app/                   │
│ dashboard-app/                  │
│ ...                             │
└─────────────────────────────────┘
```

**Environment Variables Available**:
- `${PROJECT}` - Current project name
- `${PROJECT_PATH}` - Full path to project
- `${PROJECT_BASE}` - Base directory path

---

## 7. `projects delete <group>`

**Purpose**: Delete an entire project group

**Flow**:
```
┌─────────────────────────────────┐
│ User runs: projects delete      │
│ FRONTEND                        │
└────────────┬────────────────────┘
             │
             ▼
┌─────────────────────────────────┐
│ Check if group exists           │
└────────────┬────────────────────┘
             │
      ┌──────┴───────┐
      │              │
   Not found      Found
      │              │
      ▼              ▼
   Error      Delete group
   message    from config
      │              │
      │              ▼
      │    ┌─────────────────┐
      │    │ Show: ✓ Deleted │
      │    │ group: FRONTEND │
      │    └────────┬────────┘
      │             │
      └──────┬──────┘
             │
             ▼
           Exit
```

**Aliases**: `remove`, `rm`

---

## 8. `projects clear`

**Purpose**: Clear all groups from configuration

**Flow**:
```
┌─────────────────────────────────┐
│ User runs: projects clear       │
└────────────┬────────────────────┘
             │
             ▼
┌─────────────────────────────────┐
│ Load config, count groups       │
└────────────┬────────────────────┘
             │
      ┌──────┴───────┐
      │              │
  No groups     Has groups
      │              │
      ▼              ▼
  Show: Already  Prompt:
  empty          Delete all X
      │          group(s)?
      │          (y/N)
      │              │
      │       ┌──────┴───────┐
      │       │              │
      │      No             Yes
      │       │              │
      │       ▼              ▼
      │   Cancelled    Clear all
      │       │        groups
      │       │              │
      │       │              ▼
      │       │     Show: ✓ Config
      │       │     cleared
      │       │              │
      └───────┴──────┬───────┘
                     │
                     ▼
                   Exit
```

**With --yes flag**:
```
┌─────────────────────────────────┐
│ User runs: projects clear -y    │
└────────────┬────────────────────┘
             │
             ▼
┌─────────────────────────────────┐
│ Skip confirmation, clear all    │
│ Show: ✓ Config cleared          │
└─────────────────────────────────┘
```

**Alias**: `reset`

---

## 9. `projects completion [action]`

**Purpose**: Manage shell tab completions

**Flow A: Install**:
```
┌─────────────────────────────────┐
│ User runs: projects completion  │
│ install                         │
└────────────┬────────────────────┘
             │
             ▼
┌─────────────────────────────────┐
│ Install tabtab completions      │
│ Add to shell config (~/.zshrc)  │
└────────────┬────────────────────┘
             │
             ▼
┌─────────────────────────────────┐
│ Show: ✓ Shell completion        │
│       installed!                │
│ Restart shell or run:           │
│ source ~/.zshrc                 │
└─────────────────────────────────┘
```

**Flow B: Uninstall**:
```
┌─────────────────────────────────┐
│ User runs: projects completion  │
│ uninstall                       │
└────────────┬────────────────────┘
             │
             ▼
┌─────────────────────────────────┐
│ Remove tabtab completions       │
│ Clean up shell config           │
└────────────┬────────────────────┘
             │
             ▼
┌─────────────────────────────────┐
│ Show: ✓ Shell completion        │
│       uninstalled!              │
└─────────────────────────────────┘
```

---

## Command Summary

| Command | Purpose | Interactive | Required Args |
|---------|---------|-------------|---------------|
| `init` | First-time setup | Yes (completion) | None |
| `add` | Add a group | Optional | None (interactive) or 2 |
| `add-project` | Add single project | No | 1 (group) |
| `list` | List/show groups | No | None (optional group) |
| `manage` | Manage group projects | Yes | None (optional group) |
| `run` | Execute commands | No | 2+ (group + command) |
| `delete` | Delete group | No | 1 (group) |
| `clear` | Clear all groups | Yes (confirm) | None |
| `completion` | Manage completions | No | 0-1 (action) |

---

## Typical User Journey

### First Time Setup:
```
1. projects init
   → Creates config
   → Installs completions

2. cd ~/Development/vue
   projects add
   → Interactive: name group "VUE"
   → Select projects from checkboxes

3. projects list
   → See all groups

4. projects run VUE git status
   → Run command across all VUE projects
```

### Daily Usage:
```
1. projects list VUE
   → Check what's in the group

2. projects manage VUE
   → Add/remove projects interactively

3. projects run VUE git pull
   → Update all projects

4. projects run VUE npm install
   → Install dependencies everywhere
```
